import React from 'react';

// Disabled version that doesn't expose sensitive information
const TokenDebugger: React.FC = () => {
  // Return null - component removed for security reasons
  return null;
};

export default TokenDebugger; 